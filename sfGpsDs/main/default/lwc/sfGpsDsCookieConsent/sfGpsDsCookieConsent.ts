import {
  acceptCategory,
  acceptedCategory,
  acceptService,
  acceptedService,
  validCookie,
  eraseCookies,
  getUserPreferences,
  setCookieData,
  getCookie,
  getConfig,
  validConsent,
  run,
  reset
} from "./core-api";

const CookieConsentAPI = {
  acceptCategory,
  acceptedCategory,
  acceptService,
  acceptedService,
  validCookie,
  eraseCookies,
  getUserPreferences,
  setCookieData,
  getCookie,
  getConfig,
  validConsent,
  run,
  reset
};

export default CookieConsentAPI;