We implemented the Aside component as a set of native LWCs vs. using the WebComponents because they do not support passing in slots for use in Experience Builder.

We found that being able to adapt the content markup using a slot was worth the implementation effort.
